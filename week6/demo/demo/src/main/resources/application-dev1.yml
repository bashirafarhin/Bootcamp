spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev_database
    username: root
    password: rootpass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

thread: xyx